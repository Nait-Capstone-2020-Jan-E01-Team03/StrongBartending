@page
@model StrongBartending.Pages.Configuration.EventStatu.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EventStatus[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EventStatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EventStat">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EventStat">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EventStat">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
